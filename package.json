{
  "name": "focus-app",
  "version": "1.0.0",
  "description": "Focus — app de produtividade com sistema de pontos",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static build — no bundler needed'",
    "cap:init": "npx cap init",
    "cap:add": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/app": "^5.0.0",
    "@capacitor/haptics": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
